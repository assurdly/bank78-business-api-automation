pm.test("Status code is 400", function () {
    pm.response.to.have.status(400);
});

pm.test("Status code response is Bad Request", function () {
    pm.response.to.have.status('Bad Request');
});

var jsonData = pm.response.json();

pm.test("Request Body Property", function () {
    pm.expect(jsonData).to.be.an('object');
    pm.expect(jsonData.hasResult).to.be.a('boolean');
    pm.expect(jsonData.result).to.be.null;
    pm.expect(jsonData.resultType).to.be.a('number');
    pm.expect(jsonData.message).to.be.a('string');
    pm.expect(jsonData.validationMessages).to.be.null;
    pm.expect(jsonData.successful).to.be.a('boolean');
    pm.expect(jsonData.errorCode).to.be.a('number');
    pm.expect(jsonData.responseCode).to.be.null;
});

pm.test("hasResult is false", function () {
    pm.expect(jsonData.hasResult).to.eql(false);
});

pm.test("resultType is 2", function () {
    pm.expect(jsonData.resultType).to.eql(2);
});

pm.test("message is Beneficiary details not found", function () {
    pm.expect(jsonData.message).to.eql('Beneficiary details not found');
});

pm.test("successful is false", function () {
    pm.expect(jsonData.successful).to.eql(false);
});

pm.test("errorCode is 0", function () {
    pm.expect(jsonData.errorCode).to.eql(0);
});